{
  "buildCommand": "cd client && npm run build",
  "devCommand": "cd client && npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "regions": ["iad1"],
  "routes": [
    { "src": "/(.*)", "dest": "/client/$1" }
  ]
}